export enum PricingMode {
  FIXED = 'FIXED',
  COST_PLUS = 'COST_PLUS',
  HYBRID = 'HYBRID',
}

export enum CostingMethod {
  WEIGHTED_AVERAGE = 'WEIGHTED_AVERAGE',
}

export enum Language {
  EN = 'en',
  AR = 'ar',
}

export enum Theme {
  LIGHT = 'light',
  DARK = 'dark',
}

export enum PaperWidth {
  MM_80 = '80',
  MM_58 = '58',
}

export enum PrinterType {
  USB = 'USB',
  SERIAL = 'SERIAL',
  NETWORK = 'NETWORK',
  NONE = 'NONE',
}

export enum BarcodeScannerMode {
  KEYBOARD = 'KEYBOARD',
  SERIAL = 'SERIAL',
}
